# 원자료·DB·중간산출은 올리지 않는다 (BACI zip 2.4GB, DuckDB 4GB)
/data/
*.duckdb
*.duckdb.wal

# 분석 산출물(표·그림 원본). 공개할 그림만 docs/연구/ 아래로 옮긴다
/analysis/

# 작업 문서(검토 메모, 파일럿 기록, 연구계획). 로컬 전용
/notes/

logs/
__pycache__/
*.pyc
.ipynb_checkpoints/

# 로컬 도구 설정
.claude/
CLAUDE.md
